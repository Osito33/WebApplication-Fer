@model IEnumerable<WebApplication_Fer.Models.Producto>

<div class="container">
    <div class="row">
        <div class="col-12">
            <h5 class="alert alert-primary text-center">
                Lista desplegable ejemplo
            </h5>
        </div>

        <div class="col-12"¨>
            <select>
                @foreach (var producto in ViewBag.Producto)
                {
                    <option>@producto</option>
                }
            </select>
        </div>
        
    </div>
    
</div>